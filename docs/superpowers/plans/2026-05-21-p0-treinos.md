# P0 Treinos — ✅ IMPLEMENTADO (2026-05-22)

> Tasks 1–3 (DeleteTreino, DuplicarTreino, RemoveExercicioDoTreino) foram concluídas. Resta apenas Task 4.

**Goal:** Adicionar validação de array completo em `ReordenarExerciciosUseCase` — um array parcial gera ordens duplicadas silenciosamente.

---

### Task 1: ReordenarExerciciosUseCase — validar que array contém TODOS os IDs

**Context:** `ReordenarExerciciosUseCase.ts:30-33` — valida que todo ID do input existe no treino, mas NÃO verifica que o input contém TODOS os IDs existentes. Um array parcial (ex: 3 IDs quando o treino tem 5) atribui ordens 1-3 sem mover os outros 2, criando duplicatas (dois exercícios na `ordem=2`).

**Files:**
- Create: `apps/mobile/src/application/treinos/errors/ReordenacaoIncompletaError.ts`
- Modify: `apps/mobile/src/application/treinos/use-cases/ReordenarExerciciosUseCase.ts`
- Modify: `apps/mobile/src/application/treinos/use-cases/ReordenarExerciciosUseCase.test.ts`

- [ ] **Step 1: Criar o novo erro**

Create `apps/mobile/src/application/treinos/errors/ReordenacaoIncompletaError.ts`:

```typescript
export class ReordenacaoIncompletaError extends Error {
  constructor(esperados: number, recebidos: number) {
    super(`Reordenacao incompleta: esperados ${esperados} IDs, recebidos ${recebidos}.`);
    this.name = 'ReordenacaoIncompletaError';
  }
}
```

- [ ] **Step 2: Adicionar length check ao use case**

Em `ReordenarExerciciosUseCase.ts`, após a linha `const idsExistentes = new Set(...)`, adicionar:

```typescript
import { ReordenacaoIncompletaError } from '../errors/ReordenacaoIncompletaError';

// após idsExistentes:
if (input.treinoExercicioIds.length !== existentes.length) {
  throw new ReordenacaoIncompletaError(existentes.length, input.treinoExercicioIds.length);
}
```

Atualizar o JSDoc para incluir `@throws {ReordenacaoIncompletaError}`.

- [ ] **Step 3: Escrever teste para array parcial**

Adicionar ao test file existente:

```typescript
it('throws ReordenacaoIncompletaError when array does not contain all exercise IDs', async () => {
  // setup: treino com 3 exercises, input com 2
  await expect(
    useCase.execute({ treinoId: 'treino_1', treinoExercicioIds: ['te_1', 'te_3'] })
  ).rejects.toThrow(ReordenacaoIncompletaError);
});
```

- [ ] **Step 4: Verificar TypeScript e testes**

```bash
cd apps/mobile && npx tsc --noEmit 2>&1 | head -20
npx vitest run src/application/treinos/use-cases/ReordenarExerciciosUseCase.test.ts
```
