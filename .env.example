# Postgres do docker-compose (dev local). Copie para .env e ajuste se necessário.
# O .env real é gitignored; estes são apenas defaults de desenvolvimento.
POSTGRES_USER=academia
POSTGRES_PASSWORD=academia
POSTGRES_DB=academia_db
POSTGRES_PORT=5433
